<#if item2??>
    <#if item2.submenus??>
        Submenus tiene el valor: ${item2.submenus!"Sin submenús"}
    <#else>
        Submenus no está definido o es null.
    </#if>
<#else>
    item2 no está definido.
</#if>
