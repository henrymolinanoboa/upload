            SELECT
                TOP 1
                ID,
                GUID,
                UUIDS,
                USERNAME,
                ENDPOINT,
                PAYLOAD
            FROM
                SCH_CENTRA_ID.TICKETS
            WHERE 
            STATUS = 'ABIERTO'
            ORDER BY DATE_PLANNED
